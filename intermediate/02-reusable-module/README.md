# Intermediate 02: Reusable module

This example demonstrates a root module calling a child module. Add a provider/version block before running it in a real Azure project.

Keep modules focused on one responsibility and expose stable inputs and outputs.
