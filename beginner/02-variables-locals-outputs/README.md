# Beginner 02: Variables, locals, and outputs

This example shows how to separate inputs, derived values, and useful outputs. Override defaults with a `terraform.tfvars` file or `-var` flags.

```bash
terraform plan -var="environment=test"
terraform output
```
