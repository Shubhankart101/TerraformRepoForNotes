# Beginner 01: Basic resource

Learn the Terraform lifecycle without a cloud provider. `terraform_data` is useful for learning resource syntax locally.

```bash
terraform init
terraform plan
terraform apply
terraform output message
terraform destroy
```
